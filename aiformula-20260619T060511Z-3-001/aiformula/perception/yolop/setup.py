from setuptools import setup, find_packages

setup(
    name='yolop',
    version='0.0.1',
    description='YOLOP module for object_road_detector',
    author='Your Name',
    packages=find_packages(),
    install_requires=[],
)
